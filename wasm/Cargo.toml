[package]
name = "wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
roxmltree = { workspace = true }
lib = { path = "../lib" }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
